/**
 * Interface for components that accept an optional CSS class name.
 *
 * @property {string} [className] - Optional CSS class to apply custom styling to the component.
 */
export interface WithClassname {
  className?: string;
}
